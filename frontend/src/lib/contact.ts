// Official HiSpike contact points. Single source of truth — these appear on a
// dozen pages plus the pre-rendered markup, so they live here rather than being
// retyped and drifting apart.

export const SUPPORT_EMAIL = 'support@hispike.in';

// This line takes WhatsApp messages only — no voice calls — so every surface
// that shows it has to say so. WhatsAppLink bakes that label in.
export const WHATSAPP_NUMBER = '+91 9148667842';

// wa.me wants the number bare: country code first, no plus, no spaces.
export const WHATSAPP_URL = 'https://wa.me/919148667842';
